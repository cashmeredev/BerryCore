╔══════════════════════════════════════════════════════════════╗
║                   SIMPLE GCC FIX - TRY THIS FIRST            ║
╚══════════════════════════════════════════════════════════════╝

PROBLEM: stdio.h not found by GCC 9.3.0

EASIEST FIX: Check where system headers actually are
───────────────────────────────────────────────────────────────

Step 1: Find stdio.h
─────────────────────
find /accounts/1000 -name stdio.h 2>/dev/null
find /opt -name stdio.h 2>/dev/null
find /usr -name stdio.h 2>/dev/null

Write down the path!


Step 2: Check QNX_TARGET
─────────────────────────
echo $QNX_TARGET
ls $QNX_TARGET/usr/include/stdio.h

OR try:
ls $QNX_TARGET/armle-v7/usr/include/stdio.h


Step 3: Create symlinks
─────────────────────────
If stdio.h is at: /some/path/usr/include/stdio.h

Then run:
cd $NATIVE_TOOLS
mkdir -p arm-blackberry-qnx8eabi/include
ln -s /some/path/usr/include arm-blackberry-qnx8eabi/include

# Also link to NATIVE_TOOLS/include
mkdir -p include
ln -s /some/path/usr/include/* include/


Step 4: Test
─────────────────────────
cat > /tmp/test.c << 'EOF'
#include <stdio.h>
int main() { printf("Works!\n"); return 0; }
EOF

gcc -o /tmp/test /tmp/test.c
./tmp/test


═══════════════════════════════════════════════════════════════

AUTOMATED FIX: Run the fix script
───────────────────────────────────────────────────────────────
sh FIX_GCC.sh

This will:
  1. Find where stdio.h is
  2. Create symlinks automatically  
  3. Test compilation
  4. Report success or failure


═══════════════════════════════════════════════════════════════

ALTERNATIVE: Just use the working GCC 4.6.3
───────────────────────────────────────────────────────────────
The old GCC works perfectly! You don't need 9.3.0 right now.

To revert:
sh REVERT_TO_OLD_GCC.sh

This will:
  1. Remove GCC 9.3.0
  2. Restore GCC 4.6.3
  3. Test that it works
  

═══════════════════════════════════════════════════════════════

MANUAL WORKAROUND: Compile with explicit include path
───────────────────────────────────────────────────────────────
If you find stdio.h at: /some/path/include/stdio.h

Compile like this:
gcc -I/some/path/include -o test test.c

Or set permanently:
export C_INCLUDE_PATH="/some/path/include"
export CPLUS_INCLUDE_PATH="/some/path/include"


═══════════════════════════════════════════════════════════════

WHAT TO TRY RIGHT NOW
───────────────────────────────────────────────────────────────

Option A: Try automated fix
  sh FIX_GCC.sh

Option B: Find stdio.h manually and tell me where it is
  find /accounts/1000 -name stdio.h 2>/dev/null
  find / -name stdio.h 2>/dev/null | head -20

Option C: Just go back to working GCC 4.6.3
  sh REVERT_TO_OLD_GCC.sh


╔══════════════════════════════════════════════════════════════╗
║  The 4.6.3 works great - you can use that while we fix 9.3! ║
╚══════════════════════════════════════════════════════════════╝

